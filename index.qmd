---
title: "BirdNET Homepage"
execute:
  echo: false
---
```{python}
import pandas as pd
from datetime import datetime, timedelta
import matplotlib.pyplot as plt
from birdFunctions import *

selectedData = filterData(minLim = 60)

if len(selectedData) >0:
    for _, row in selectedData.iloc[::-1].iterrows():
        if row['species'] not in exclude:
            print(f"{row['timestamp']} — {row['species']} (confidence: {row['confidence']:.2f})")
else:
    print("No observations in the last hour")
```